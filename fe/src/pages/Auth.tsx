

    export default function Auth(){
        function Login(){
            
        }
        return <div>
            <button onClick={()=> Login("google")}>Login with google</button>
            <button onClick={()=> Login("google")}>login with github</button>
        </div>

    }